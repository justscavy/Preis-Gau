class objects:
   def __init__(self, product, color, weight):
           self.product = product
           self.color = color
           self.weight = weight

   def introduce_self(self):
               print("Price for product " + self.product) #this in java

self.product = input("Name of product your looking for?: " + )

r1 = objects("tomaten", "red", 30)
r2 = objects("kirschen", "blue", 40)

r1.introduce_self()
